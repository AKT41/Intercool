import productSchema from './product-schema'
import referenceSchema from './ref-schema'
import homeSlider from './homa-slider'

const schemas = [productSchema, referenceSchema, homeSlider]

export default schemas
